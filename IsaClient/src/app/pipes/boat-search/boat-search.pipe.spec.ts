import { BoatSearchPipe } from './boat-search.pipe';

describe('BoatSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new BoatSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
